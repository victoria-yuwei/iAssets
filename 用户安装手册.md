# iAssets 用户安装与设置手册

本手册面向想把 **iAssets** 装到自己 iPhone 上使用的用户。  
当前版本通过 **Xcode 安装到真机**（开发安装），不经过 App Store。

参考设备：iPhone · iOS 17 及以上（文档撰写时验证参考为 iPhone 17e / iOS 26.x）。

---

## 1. 你需要准备什么

| 项目 | 说明 |
| --- | --- |
| Mac | 一台能安装 **Xcode** 的 Mac（仅有「命令行工具」不够，需要完整 Xcode） |
| Xcode | 从 Mac **App Store** 搜索并安装「Xcode」，首次打开按提示安装额外组件 |
| Apple ID | 普通 Apple ID 即可开始；若要用完整 iCloud/CloudKit 能力，建议使用已加入 **Apple Developer** 的账号（个人免费账号也可把 App 装到自己的手机，但证书有效期较短，过期需重新安装） |
| iPhone | 已更新到支持的系统版本；准备原装或合格的数据线 |
| 工程文件 | 本仓库中的 `iAssets.xcodeproj` |

---

## 2. 设置 iPhone（先做这一步）

### 2.1 解锁并连接电脑

1. 用数据线把 iPhone 连到 Mac。  
2. 手机弹出「要信任此电脑吗？」时，点 **信任**，并输入手机密码。  
3. 若使用无线调试，可先有线连上一次，再在 Xcode 里启用网络调试（可选）。

### 2.2 打开开发者模式（iOS 16 及更新系统通常需要）

1. 打开 iPhone：**设置 → 隐私与安全性**。  
2. 滑到下方，找到 **开发者模式**。  
3. 打开开关，按提示 **重启** 手机。  
4. 重启后再次确认开启开发者模式。

> 若暂时看不到「开发者模式」：先用 Xcode 尝试运行一次到真机，系统往往会在之后自动出现该选项，再回来打开即可。

### 2.3 保持网络可用（首次安装与日常使用）

- 安装过程：手机与 Mac 建议在可联网环境（证书校验、依赖下载）。  
- 使用汇率与 iCloud 同步时：手机需要能访问网络。  
- 仅查看本地已录入资产时：可离线使用（汇率显示为上次缓存）。

### 2.4 （推荐）登录 iCloud

若你希望换机不丢数据、多设备同步：

1. **设置 →** 点顶部 Apple ID。  
2. 确认已登录，并打开 **iCloud**。  
3. 确保 iCloud 有可用空间。  

在 App 内「我的」里也可再确认 iCloud 同步开关（默认建议开启）。

---

## 3. 在 Mac 上用 Xcode 把 App 装进手机

### 3.1 安装并打开工程

1. 打开 **Xcode**。  
2. 菜单 **File → Open…**，选择项目里的：  
   `iAssets.xcodeproj`  
3. 等待 Xcode 完成索引（左上角进度结束即可）。

### 3.2 配置签名（Signing）

1. 左侧点开蓝色工程图标 **iAssets**。  
2. 选中 TARGETS 下的 **iAssets**。  
3. 打开 **Signing & Capabilities**。  
4. 勾选 **Automatically manage signing**。  
5. **Team** 里选择你的 Apple ID 对应团队：  
   - 若列表为空：点 **Add Account…**，登录 Apple ID。  
6. 确认 **Bundle Identifier** 为 `app.iassets.ios`（若提示与别人冲突，可改成例如 `com.你的名字.iassets`，改完保持唯一即可）。  
7. 确认已有 **iCloud** 能力（工程已带 entitlements）。若 Xcode 提示需要在开发者后台创建容器，按提示启用 CloudKit / iCloud 即可。

### 3.3 选择你的 iPhone 作为运行目标

1. 用线连接手机并解锁。  
2. Xcode 窗口顶部设备列表中，选择你的 **iPhone 名称**（不要选 Simulator，除非你只想先在模拟器试玩）。  
3. 若设备发灰或不可选：检查数据线、信任提示、是否已开启开发者模式。

### 3.4 编译并安装

1. 点击左上角 **▶ Run**（或快捷键 `Command + R`）。  
2. 等待编译完成；成功后 App 会出现在手机桌面。  
3. 若报错：把 Xcode 红色错误信息记下（常见是签名 Team 未选、Bundle ID 冲突、开发者模式未开）。

### 3.5 第一次在手机上打开：信任开发者

用个人 Apple ID 安装时，系统会拦截未验证的开发者应用，需要手动信任一次：

1. iPhone 打开：**设置 → 通用 → VPN 与设备管理**（有的系统文案为「设备管理」/「描述文件与设备管理」）。  
2. 在 **开发者 App** 下找到你的 Apple ID / 开发者证书。  
3. 点进去，选择 **信任**，再次确认。  
4. 回到桌面，重新打开 **iAssets**。

之后一般无需重复信任（除非换了开发者账号或重新签名安装）。

---

## 4. 进入 App 后怎么完成初始设置

按引导页操作即可：

1. **了解产品**：万物资产化、日耗、变现。  
2. **选择本位币**：默认人民币（CNY），可随时在「我的」里改；改完后金额会按汇率统一换算。  
3. **iCloud**：建议保持开启，便于备份与换机。  
4. 进入主界面后：  
   - 点底部 **添加**，录入第一件资产；或  
   - 到 **我的 → 导入**，用 CSV/JSON 批量导入。

### 4.1 五个主要入口（速览）

| Tab | 用途 |
| --- | --- |
| 总览 | 看总净值、汇率时间、分类结构、趋势 |
| 陈列柜 | 浏览、搜索、筛选你的资产 |
| 添加 | 新建一件资产（可配图） |
| 复盘 | 看卖出盈亏、日耗排行、闲置提醒 |
| 我的 | 本位币、汇率刷新、iCloud、导入导出 |

### 4.2 建议的日常用法

1. 买到新东西 → **添加**里入库。  
2. 偶尔打开 **总览** 看家当净值。  
3. 想知道「值不值」→ 进详情看 **日均成本**。  
4. 卖掉了 → 详情里 **登记变现**。  
5. 换手机前确认 **iCloud 已开**，或先在「我的」里 **导出备份**。

---

## 5. 常见问题

### Q1：桌面上有图标，一点就闪退或提示未受信任？

到 **设置 → 通用 → VPN 与设备管理** 里信任开发者证书（见上文 3.5）。

### Q2：Xcode 提示无法验证开发者 / 签名失败？

- 确认选了正确的 **Team**。  
- Bundle ID 改成不会冲突的唯一值。  
- 手机系统时间自动设置正确。  
- 免费个人账号安装的 App 大约 **7 天** 后可能无法打开，需重新用 Xcode 安装一次。

### Q3：找不到开发者模式？

先成功用 Xcode 对真机 Run 一次，再回 **设置 → 隐私与安全性** 查找。

### Q4：汇率一直不更新？

- 确认手机联网。  
- 在 **总览** 或 **我的** 里手动点刷新。  
- 无网时会使用缓存汇率，并显示上次更新时间。

### Q5：换机后数据没有了？

- 新机登录 **同一个 Apple ID**，打开 iCloud。  
- App 内确认 **iCloud 同步已开启**，稍等同步完成。  
- 若从未开过 iCloud，只能依靠你之前导出的 JSON/CSV 再导入。

### Q6：我没有 Mac，能不能装？

目前该工程以 Xcode 真机安装为主。没有 Mac 时，需要借用一台 Mac 完成编译安装，或等待日后若提供 TestFlight / App Store 分发后再用手机直接安装。

---

## 6. 安全与隐私提示（请用户知晓）

- iAssets **没有**单独的注册登录账号。  
- 资产明细 **不会**上传到开发者自己的服务器。  
- 开启 iCloud 后，数据保存在 **你的 Apple ID / iCloud** 中，请妥善保管 Apple ID。  
- 卸载 App 前，建议先 **导出备份**，或确认 iCloud 中已有同步副本。

---

## 7. 安装检查清单（可打印对照）

- [ ] Mac 已安装完整 Xcode  
- [ ] 已用 Xcode 打开 `iAssets.xcodeproj`  
- [ ] Signing 已选 Team，签名无红色报错  
- [ ] iPhone 已信任此电脑  
- [ ] 已开启开发者模式（如系统要求）  
- [ ] Xcode 顶部已选中你的 iPhone  
- [ ] Run 成功，桌面出现 iAssets  
- [ ] 已在「VPN 与设备管理」中信任开发者  
- [ ] （可选）已登录 iCloud 并在 App 内开启同步  
- [ ] 已完成引导：本位币设置完毕  

完成以上步骤后，即可正常使用 iAssets。
